import { createBrowserHistory } from 'history';

/**
 * ### history
 * 
 * createBrowserHistory() => _Is for use in modern web browsers that support the 
 * [HTML5 history API](http://diveintohtml5.info/history.html)_
 * 
 * @author NAR
 * @version 1.0.1
 * @since 18.1
 */
export const history = createBrowserHistory();